<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.kh.spring.dao.AdminDAO">
	<select id="selectProductList" resultType="kr.kh.spring.vo.ProductVO">
		select * from product
	</select>
	<insert id="insertFile">
		insert into `file`(fi_name, fi_pr_code, fi_ori_name) 
		values(#{file.fi_name},#{file.fi_pr_code},#{file.fi_ori_name})
	</insert>
	<insert id="insertProduct">
		insert into 
	</insert>
</mapper>